<?xml version="1.0" encoding="utf-8"?>
<LayeredImageDrawable
  isNinePatch="false"
  scaleUp="true"
>
  <!-- Each src png file contains four colours (RGBA, which we call colour0-3). Each of these is replaced with the value given.
  If you write a name starting with an @ symbol, the system will look for the actual value from first the theme then the profile.
  This means you replace each channel of the original png with a colour of your choice.
  Blending works like this:
    (source_Red * colour0) + (source_Green * colour1) + ... etc -->
  <Layer src="LinearGradientBackground.png" colour0="@GradientBackgroundLeft" colour1="@GradientBackgroundRight" colour2="@GradientBackgroundTop" colour3="@GradientBackgroundBottom"/>
</LayeredImageDrawable>