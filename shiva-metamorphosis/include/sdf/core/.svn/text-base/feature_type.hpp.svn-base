#ifndef SDF_CORE_FEATURE_TYPE_INCLUDED
#define SDF_CORE_FEATURE_TYPE_INCLUDED

namespace sdf
{
	//----------------------------------------------------------------------------------------------------------------------
	/// @enum Feature types of a closest hit. This allows to handle the cases of edges and vertices
	//----------------------------------------------------------------------------------------------------------------------
	enum feature_type
	{
		vertex_a = 0,
		vertex_b,
		vertex_c,
		edge_ab,
		edge_bc,
		edge_ca,
		face,
		max_close_type
	};
}

#endif // SDF_CORE_FEATURE_TYPE_INCLUDED